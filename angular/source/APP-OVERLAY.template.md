# App: APP_NAME

Replace this file when creating your app overlay (e.g. `my-app.mdc`) and fill in:

- App purpose (agent desktop, supervisor portal, etc.)
- Key feature modules and routes
- Backend services used (REST, GraphQL, Socket.io endpoints)
- App-specific services or shared libs
