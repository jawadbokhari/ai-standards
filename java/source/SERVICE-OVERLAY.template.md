# Service: SERVICE_NAME

Replace this file when creating your service overlay (e.g. `my-service.mdc`) and fill in:

- Domain library: `com.ef.cim.objectmodel.*` (or your service's shared library)
- Service-specific packages: list domain packages unique to this service
- Key flows: describe main entry points (e.g. webhook handlers, event factories)
